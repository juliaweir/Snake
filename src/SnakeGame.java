public class SnakeGame {
    public static void main(String[] args) {
    //    GameFrame frame = new GameFrame(); just rewrite this in one line (Shown below)
        new GameFrame(); //still creates instance of Gameframe
    }
}
